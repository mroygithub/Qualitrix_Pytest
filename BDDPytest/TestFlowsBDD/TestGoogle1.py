from pytest_bdd import scenario, given, when, then




Given("user launch google application, () => {
	
});

Then(/^user validate google logo$/, () => {
	return true;
});

Then(/^user validate search test box$/, () => {
	return true;
});

Then(/^user close the application$/, () => {
	return true;
});
